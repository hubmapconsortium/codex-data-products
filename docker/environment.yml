name: codex-data-products
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.8,<3.10
  - pip
  - anndata==0.9.2
  - pip:
      - leidenalg
      - manhole
      - pandas
      - pyyaml
      - requests
      - scanpy
      - zarr
      - muon
      - tables